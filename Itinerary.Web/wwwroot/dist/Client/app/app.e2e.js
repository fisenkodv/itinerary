describe('App', function () {
    beforeEach(function () {
        // browser.get('/');
    });
    it('e2e test should run', function () {
        var subject = true;
        var result = true;
        expect(subject).toEqual(result);
    });
});
//# sourceMappingURL=app.e2e.js.map